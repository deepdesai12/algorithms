void mySort(int arr[], unsigned int n);

I would write mySort() as followed:
First to include the mySort method I would write #include "filename.extension" before the main method. To call the function I would betterSort(int data[], first, last);. The given function is used, where the 'last' is 1 less than the total number of elements 


